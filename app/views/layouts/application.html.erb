<!DOCTYPE html>
<html>
<head>
  <title>SpyREST - Beautiful auto generated REST API documentation</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <%= csrf_meta_tags %>
  <%= javascript_include_tag "vendor/modernizr"%>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="//maxcdn.bootstrapcdn.com/font-awesome/4.2.0/css/font-awesome.min.css" rel="stylesheet">
</head>
<body class="<%= controller.class.to_s.underscore.gsub(/_controller$/, '')%>">

  <header class="row full-width top-nav-container">
    <div class="small-2 columns">
      <%= link_to root_path, title: 'SpyREST' do %>
      <%= image_tag 'logo.png' %>
      <% end %>
    </div>
    <div class="small-8 columns"></div>
    <div class="small-2 columns">
      <nav class="top-bar" data-topbar role="navigation">
        <ul class="title-area">
          <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
        </ul>

        <section class="top-bar-section">
          <ul class="right">
            <li><%=link_to 'API Docs', api_hosts_path %></li>
            <li><%=link_to 'GitHub', 'https://github.com/smsohan/api_through_ui'%></li>
            <% if user_signed_in? %>
            <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
            <% else %>
            <li><%= link_to "Login", new_user_session_path unless current_user %></li>
            <% end %>
          </ul>
        </section>
      </nav>
    </div>
  </header>

  <div class="row full-width breadcumbs-row">
    <div class="small-12 small-centered columns">
      <%= render_breadcrumbs %>
    </div>
  </div>

  <div class="row full-width">
    <div class="small-12 small-centered columns">
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
    </div>
  </div>

  <div class="row full-width">
    <div class="small-12 small-centered columns">
      <%= yield %>
    </div>
  </div>

  <footer class="text-center spaced">
    <%= image_tag 'footer.png', class: 'footer-image' %>
  </footer>


</body>
</html>
